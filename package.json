{
  "name": "xray-classifier",
  "version": "1.0.0",
  "description": "X-Ray image intensity classification using AI",
  "scripts": {
    "setup": "python3 -m venv venv && . venv/bin/activate && python3 -m pip install --upgrade pip && python3 -m pip install -r requirements.txt",
    "start": ". venv/bin/activate && python3 app.py",
    "dev": ". venv/bin/activate && python3 app.py"
  },
  "dependencies": {},
  "devDependencies": {},
  "private": true
}